package compiler488.ast;

public interface Readable extends Locatable
{

    /*
     * Classes that extend this interface can be used
     * as arguments to ReadStmt. Don't confuse this
     * concept with the printing of the
     * AST itself.
     */
}
